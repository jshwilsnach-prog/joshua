# Cutover

The door is **https://nekyia.me**. That name stands without grok.me.

One Cloudflare Worker (`nekyia`): static rooms, `/api/aught` (KV), `/api/walk` (relay). See `HOUSE.md` and `TALLY.md`.

`src/game/law.ts` is unchanged.
