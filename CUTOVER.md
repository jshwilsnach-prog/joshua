# Cutover

The door is **https://nekyia.me**. That name stands without grok.me.

One Cloudflare Worker: static rooms, `/api/aught` (KV), `/api/walk` (relay). See `HOUSE.md` and `TALLY.md`.

`src/game/law.ts` is unchanged.

Workshop preview may still exist. Do not share it as the word.
