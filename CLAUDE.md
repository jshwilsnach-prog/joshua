# Claude may program here

This file is permission, not a throne.

Claude Code (local session, `claude rc` / `/rc`) may read the tree, run tests, edit files, and commit on a branch that is not `main`.

Table stays Josh. Grok is an independent adversarial reader. The door is https://nekyia.me. The house is this repo. Law is `src/game/law.ts`.

## Do

- Work on a branch. Push the branch. Do not push `main`. Pushing `main` deploys the Worker.
- Inspect first. Current live main (door) at the time this was written: `1f3debca97a91dcf7825a8787a0afd6c8b9dcc67`.
- Before more design: `npm ci && npm run build:house`, then read `dist/assets/*.css`. Vite house root is `house/`. Styles live in `src/styles.css` via `house/main.tsx`. Check whether the built CSS contains `.inset-0{` and how large it is. Compare to live `nekyia.me`.
- Verify tests yourself (`npm test`). Do not trust a note that the CSS or the relay bound already works.
- Drop over-budget walk messages. Do not close sockets. No IPs. No identity.

## Do not

- Do not edit `src/game/law.ts` or `src/game/pit.ts` or game content unless Table asks.
- Do not add inter-model rails, a second GitHub throne, or a vendor as the door.
- Do not collect seed words. Do not retry seed UI until buttons work.
- Do not treat a lintel README as the live rooms.
